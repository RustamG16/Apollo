interface:
  display_name: "Concept Studio"
  short_description: "Detail the one redesign direction the brief specifies"
  default_prompt: "Use $concept-studio to detail the one direction the approved brief's direction block specifies. Produce alternatives only if the user asked."
