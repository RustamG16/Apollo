---
id: inv-002
title: "42 skills, 7 crews, one workspace"
source: src-0002 — https://www.instagram.com/p/DbcJ2RUjPK4/ (ibraviz.ai)
evidence: 8 OCR'd carousel slides, library/frames/src-0002/
mnemosyne_verdict: park (refines-existing)
filed: '2026-08-28'
---

# ibraviz.ai — 42 skills, 7 crews

Skills grouped by **business function** rather than by technology. Roughly six per
department, each with a one-line job description and a role label ("your build team",
"your design studio").

## Department 01 — Developers · *ship the code, scaffold to QA*

`superpowers` (planning and TDD, enforced) · `context7` (docs that match your version) ·
`mcp-builder` (wires Claude into any tool) · `skill-creator` (scaffolds your own skills) ·
`webapp-testing` (drives the browser for you) · `claude-mem` (carries memory between sessions)

## Department 02 — Design · *UI that never looks templated*

`frontend-design` (React and Tailwind with taste) · `web-artifacts` (shadcn components on
demand) · `canvas-design` (straight to PNG or PDF) · `algorithmic-art` (generative p5.js
pieces) · `ui-ux-pro-max` (full design-system reasoning) · `slack-gif` (animated GIFs,
Slack-ready)

Departments 03–07 (marketing, social, finance, legal, operations) were partly resolved by
OCR; slide numbering jumps 05→07 and "operations" is inferred from an `OPERAT` fragment.

---

## Assessment

**Verdict from the pipeline: park.** As a shopping list it is mostly duplicate — the
entire marketing and social departments already exist locally (`seo-audit`, `ai-seo`,
`ad-creative`, `cro`, `programmatic-seo`, `marketing-psychology`, `copywriting`, `social`,
`content-strategy`, `video`, `emails`). Design is covered by `frontend-design`,
`impeccable`, `web-design-pro` and apollo's own set.

## What it is actually good for

**A coverage check, not an acquisition list.** Laying your ~97 skills against a
seven-department grid exposes which departments are thin. Doing that here surfaces one
honest question: **finance and legal have no local coverage at all.** That is either a
real gap or correctly out of scope — but it should be a decision, not an accident.

Note the structural echo with [`src-0005`](../04-ui-references/system-map-ui.md): same
seven-department mechanism, different creator, different month. The taxonomy is
convergent; treat "7 departments" as a genre convention rather than a discovery.
