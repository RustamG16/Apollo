.DS_Store
Thumbs.db
node_modules/
.env
.env.*
!.env.example
tmp/
*.log
apollo-studio/data/*.jsonl

# test_projects/* are device-specific, each an independent git repo with its
# own origin (or none yet) — never tracked or pushed as part of Apollo.
test_projects/
