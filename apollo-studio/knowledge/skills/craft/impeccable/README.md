# Impeccable

- Category: Craft
- Phase: build
- Status: active
- Routed: yes
- Skill ID: impeccable

Own the finish: commit the design system as numbers before layout (PRODUCT.md, DESIGN.md, design.json), then run the bounded craft and review pass over the implementation. Its scripts/ and reference/ playbooks ship in this library, so it must be projected as a body rather than left to a host plugin.

## Runtime instructions

Use only when explicitly routed for an additional finishing pass. Resolve hierarchy, typography, spacing, states, accessibility, responsive behavior, and purposeful interaction without overriding the approved UI/UX, Apple, or Emil design contracts.
