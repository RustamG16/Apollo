# gsap-utils

- Category: Motion
- Phase: prepare
- Status: active
- Routed: yes
- Skill ID: gsap-utils

Official GSAP skill for gsap.utils — clamp, mapRange, normalize, interpolate, random, snap, toArray, wrap, pipe. Use when the user asks about gsap.utils, clamp, mapRange, random, snap, toArray, wrap, or helper utilities in GSAP.

## Runtime instructions

Reach for `gsap.utils` helpers - clamp, mapRange, interpolate, snap, wrap, random, toArray - when motion math would otherwise be hand-rolled. Utilities only; they do not by themselves justify adding animation.
