# Three.js Implementation

- Category: Spatial
- Phase: build
- Skill ID: three-js-implementation

Implements bounded Three.js scenes with lifecycle, loading, accessibility, and static fallbacks.

## Runtime instructions

Implement Three.js only after a WebGL concept is approved. Scope renderer lifecycle, dispose resources, cap device pixel ratio, provide reduced-motion and static fallbacks, and keep core content accessible without the canvas.
