# QA evidence

## Apollo Workbench prototype — 2026-08-31

- Environment: existing local Node server at `http://127.0.0.1:4173/#/work`, demo mode, Codex in-app browser.
- `npm.cmd run check` passed; `/api/health` returned `ok: true`, `mode: demo`, and `oracle: true`; project-context validation passed.
- Desktop Work: project workspace, tabs, messages, composer, context inspector, and Oracle dock rendered with no browser console errors.
- Keyboard: Enter sent a local demo message; Shift+Enter retains the native line-break path.
- Navigation: Work opened the preserved Systems surface and Library opened the existing Knowledge surface.
- Narrow 390×844: project sidebar hid, composer remained present, and document `scrollWidth` equalled `clientWidth` (380 px).
- Repair cycle 1: closed context drawer initially expanded document width to 733 px; narrow overflow containment fixed it and was rechecked.

Remaining: OS preference simulation for reduced motion/transparency, axe, packaged Windows smoke, and full keyboard traversal are Phase 2 checks. This controlled-fixture prototype has no SQLite persistence, attachment permission, live streaming/stop, or persisted proposal application.

## Automated

- `node --check`: server, skill registry, and client script pass.
- `/api/health`: returns HTTP 200 and current runtime mode.
- `/api/config`: returns 18 skills, six declared adapters, four presets, and three current model choices.
- `/api/compare`: two demo variants execute concurrently and return labeled output, latency, and zero-token demo usage.

## Browser pass 1

- Desktop 1440px: seven connector paths rendered; all had non-zero geometry.
- Playground: two same-prompt results rendered with mode, latency, skill count, and output.
- Mobile 390px: no horizontal overflow; workflow converted to an ordered stack.
- Findings: external GSAP CDN was blocked and favicon was missing.

## Repairs

- Vendored GSAP 3.15.0 locally.
- Added local favicon.
- Demo mode now reports zero currently runnable tools.

## Browser pass 2

- Zero console or page errors.
- Skill count changed from six to seven after switching Award Rubric and persisted after reload.
- Phase filtering dimmed five unrelated connector paths.
- Three variants executed and produced three result cards.
- Run history incremented.
- Mobile remained exactly 390px wide with no overflow.

Screenshots: `.olympus/review/desktop-architecture.png`, `desktop-playground.png`, `desktop-final.png`, and `mobile-architecture.png`.

## Known boundary

Live provider behavior requires a valid `OPENAI_API_KEY` and was not invoked during QA. MCP adapters other than hosted web search are correctly shown as unavailable until authenticated bridges exist.

## Persistent Work slice — 2026-09-01

- `npm.cmd run check` passed after adding the local workspace adapter and approval dialog.
- Project-context validation passed.
- API smoke test passed against the running Node server: health check, project/chat creation, transcript persistence, explicit attachment metadata registration, proposal creation, and cancellation all returned expected results.
- Test records were removed after the smoke test; only the seeded WKO workspace remains in `data/workspace.json`.
- Browser automation was unavailable in this host, so no new screenshot or visual pass is claimed for this incremental functional slice. The existing desktop/narrow visual evidence remains the last browser-backed evidence.

Open verification: perform the Work attachment/unlink review path in an interactive browser at desktop and 390 px before Gate C.

## Extension QA — knowledge, agents, and Oracle

- `npm.cmd run check`: server, client, skill contracts, knowledge registry, and agent planner pass syntax validation.
- Startup materialized 19 categorized skill folders, including a custom `Spatial/Three.js Implementation` skill created through the UI.
- Added a Markdown source note through the Knowledge UI and verified its physical file path.
- `/api/oracle/plan`: whole-term matching avoided substring false positives; all seven genuinely matched specialists stayed visible under a constrained 30,000-token budget through proportional allocation.
- `/api/oracle/chat`: demo response performed no model calls, listed only activated specialists, and left Spatial and Media agents waiting for explicit approval.
- Oracle chat, plan, agent registry, knowledge categories, and host diagnostics rendered with zero console warnings or errors.
- Wide desktop at 1440px: Oracle split into an 890px chat surface and 430px plan/integration rail with no body overflow.
- Mobile at 390px: Knowledge and Oracle were 344px wide inside a 390px viewport; no page-level horizontal overflow. Navigation and category rails use intentional horizontal scrolling.
- Architecture connector screenshot confirmed corrected node-edge geometry after the extension.

## Desktop continuous-improvement pass — 2026-09-01

### Contract and environment

- Scope: laptop and desktop only, per the user’s explicit constraint. Tested at 1280×800, 1440×900, and 1920×1080 in the Codex in-app browser against `http://127.0.0.1:4173` in demo mode.
- Routes: Work, Architecture, Agent Profiles, and Playground.
- Build inputs: the approved Apollo Workbench direction, `.olympus/05-asset-manifest.md`, and `.olympus/12-continuous-improvement-plan.md`.

### Automated and runtime evidence

- `scripts/validate-project-context.ps1 -ProjectPath .` passed.
- `npm.cmd run check` passed for the server, MCP adapter, events, systems, client, skills, knowledge, and agents modules.
- `/api/health` returned `ok: true`, `mode: demo`, `oracle: true`, and 25 enabled skills.
- Browser console/runtime log collection returned zero errors or warnings after the implementation and repair pass.
- Both generated WebP assets loaded in the verified pages; five of five Agent Profile portraits rendered at 1440×900.

### Browser pass and repair cycle 1

- Work at 1440×900 initially ended 38 px below the viewport because the full-height workspace did not account for the existing main top offset. The composer was partly below the fold.
- Repair: the desktop Work height now accounts for the clamped main offset, and Work-active main content removes the legacy 80 px bottom padding.
- Recheck at 1280×800: document width and height matched the viewport (1280×800), there was no horizontal overflow, and the composer bottom was 780 px. The four context cells remained 173 px wide and readable.
- Recheck at 1920×1080: no overflow, document height matched 1080 px, the inspector remained 300 px, and the composer bottom was 1060 px.

### Interaction and accessibility evidence

- Lucide command icons rendered locally (17 SVG command instances on tested views) and buttons retain visible text labels.
- The node toolbar exposes one tab stop at a time. ArrowRight moved focus from Connect to Auto layout and updated the roving tabindex contract.
- A selected workflow node moved with ArrowRight, announced its new position in the status region, and returned to its exact starting position with ArrowLeft.
- Delete on the core Intake node preserved all eight workflow nodes, disabled the inspector delete command, and announced that only custom nodes are removable.
- Connect mode visibly toggled `aria-pressed` and returned to a neutral cancelled state after the interaction test.
- Every workflow node exposes keyboard shortcut metadata and the canvas references screen-reader instructions for Arrow, Shift+Arrow, Delete, and Escape.
- The comparison pilot produced two labeled demo results and advanced the evidence progress state to complete.
- Current browser preference reported full motion and GSAP registered that preference. Reduced-motion behavior is implemented in both CSS and JavaScript, but OS-level media emulation was unavailable in this browser control surface, so no simulated reduced-motion screenshot is claimed.

### Layout evidence

- Playground at 1280×800 and 1920×1080 had no horizontal overflow. At 1920 px the task rail was 494 px, the comparison field 960 px, and each setup card 454 px.
- Agent Profiles at 1440×900 rendered as two 665 px columns with no horizontal overflow.
- Generated functional media is present behind Work orientation and Playground comparison surfaces without carrying text, UI chrome, or interaction meaning.

### Remaining release boundaries

- No mobile or narrow-phone work is included in this pass by explicit scope decision.
- The locally vendored Lucide UMD file is 423,290 bytes before transfer compression. A future bundler migration should tree-shake it to the used icon set; it is accepted for this dependency-light local prototype.
- Live provider behavior, packaged Windows smoke testing, and OS-level reduced-motion simulation remain outside this browser-demo Gate C pass.

## Visual-enrichment verification — 2026-09-01

- Scope: desktop visual treatment for Systems, Architecture, and the Knowledge Factory at 1440×900 in the Codex in-app browser.
- `npm.cmd run check` and `scripts/validate-project-context.ps1 -ProjectPath .` both passed after the media and style changes.
- `system-concordance-01.webp` is present in both the Systems hero and Architecture canvas background stack; `knowledge-reliquary-01.webp` is present in the Knowledge Factory hero. Both were served locally and loaded successfully.
- Systems and Knowledge Factory each gained a dedicated, low-opacity visual scene while their headings, actions, filters, editor fields, and content remain foreground layers. The Architecture canvas stays 590 px high and preserves its node surface.
- At the tested width, document `scrollWidth` equalled `clientWidth` (1430 px) on Systems, Knowledge Factory, and Architecture; no console errors or warnings were recorded.
- Reduced-transparency and high-contrast rules remove the decorative secondary layers while preserving content contrast. OS-level preference emulation remains unverified.
