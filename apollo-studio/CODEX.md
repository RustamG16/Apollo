# CODEX — Apollo Studio router

1. Always read `ARCHITECTURE-ESSENTIALS.md` before making a change.
2. For product scope, audience, exclusions, or success signals, read `PRD.md`.
3. For runtime, data, migration, bridge, build, or deployment, read `ARCHITECTURE.md`.
4. For work boundaries, protected assets/data, frontend quality, or release checks, read `AGENTS.md`.
5. For decisions and approvals, read `PROGRESS-AND-DECISIONS.md` and `.olympus/run.json`.
6. For the approved master delivery sequence, read `.olympus/10-desktop-app-master-plan.md`.

Commands:

```powershell
npm.cmd run check
npm.cmd start
..\scripts\validate-project-context.ps1 -ProjectPath .
```
